# PratikoAI Backend - PREPROD Environment Configuration Example
# Copy this to .env.preprod and fill in your actual values
#
# PREPROD Environment: Pre-production simulation that MIRRORS production config
# Use this for final validation before deploying to production
# Formerly known as "test" (but NOT for unit/integration tests - those use "development")

# Environment
APP_ENV=preprod
DEBUG=false
LOG_LEVEL=WARNING
LOG_FORMAT=json

# Database Configuration (Use SSL like production)
POSTGRES_URL=postgresql+asyncpg://username:password@preprod-db-host:5432/pratikoai_preprod?ssl=require

# JWT Configuration (Same as production)
JWT_SECRET_KEY=your-preprod-jwt-secret-key-must-be-very-secure-and-long
JWT_ACCESS_TOKEN_EXPIRE_HOURS=168  # 7 days (same as production)
JWT_REFRESH_TOKEN_EXPIRE_DAYS=90   # 90 days (same as production)

# LLM Configuration
LLM_API_KEY=your-preprod-openai-api-key
LLM_MODEL=gpt-4o-mini
ANTHROPIC_API_KEY=your-preprod-anthropic-api-key

# Redis Configuration (Use Redis cluster like production)
REDIS_URL=redis://preprod-redis-cluster:6379/0
REDIS_PASSWORD=your-redis-password

# Email Configuration for Metrics Reports
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=pratikoai-preprod@company.com
SMTP_PASSWORD=your-preprod-app-password
FROM_EMAIL=preprod@pratikoai.com

# PREPROD Metrics Report Recipients
METRICS_REPORT_RECIPIENTS=preprod-alerts@pratikoai.com,ops-team@pratikoai.com
METRICS_REPORT_RECIPIENTS_ADMIN=admin@pratikoai.com,tech-lead@pratikoai.com
METRICS_REPORT_RECIPIENTS_TECH=tech-alerts@pratikoai.com,devops@pratikoai.com
METRICS_REPORT_RECIPIENTS_BUSINESS=business-metrics@pratikoai.com

# Strict Rate Limiting (SAME AS PRODUCTION)
RATE_LIMIT_DEFAULT=200 per day,50 per hour

# Stripe Configuration (Test Mode for PREPROD)
STRIPE_SECRET_KEY=sk_test_your_preprod_stripe_secret_key
STRIPE_PUBLISHABLE_KEY=pk_test_your_preprod_stripe_publishable_key
STRIPE_WEBHOOK_SECRET=whsec_your_preprod_webhook_secret
STRIPE_MONTHLY_PRICE_ID=price_your_preprod_test_price_id

# Vector Database Configuration
PINECONE_API_KEY=your-preprod-pinecone-api-key
PINECONE_ENVIRONMENT=your-preprod-environment
PINECONE_INDEX_NAME=pratikoai-knowledge-preprod

# Security Configuration (Same strictness as production)
API_KEYS={"admin": "preprod-admin-key-rotate-monthly", "service": "preprod-service-key-rotate-monthly"}
API_KEY_ROTATION_DAYS=30

# Monitoring and Tracing
LANGFUSE_PUBLIC_KEY=your-preprod-langfuse-public-key
LANGFUSE_SECRET_KEY=your-preprod-langfuse-secret-key

# Production-like settings (PREPROD mirrors production)
ENABLE_PERFORMANCE_MONITORING=true
ENABLE_ERROR_TRACKING=true
BACKUP_ENABLED=true
SSL_REDIRECT=true

# RAG Trace Logging (DISABLED in PREPROD - mirrors production)
# No per-request RAG trace files in PREPROD (same as production)
